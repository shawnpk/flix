<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= movie.title %> ()<%= movie.rating %>): <%= number_to_currency(movie.total_gross) %>
    </li>

    <p><%= movie.description %></p>

    <p><%= movie.released_on %></p>
  <% end %>
</ul>
